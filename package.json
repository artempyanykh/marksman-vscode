{
  "name": "zeta-note",
  "displayName": "Zeta Note",
  "description": "Delightful note taking in Markdown with cross-references and diagnostics.",
  "icon": "assets/logo.png",
  "galleryBanner": {
    "color": "#dbeae9",
    "theme": "light"
  },
  "publisher": "arr",
  "version": "0.0.1",
  "license": "SEE LICENSE IN LICENSE",
  "engines": {
    "vscode": "^1.54.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "markdown",
    "note",
    "notes",
    "link",
    "links",
    "references",
    "crossreference",
    "zettelkasten",
    "roam",
    "lsp"
  ],
  "activationEvents": [
    "onLanguage:markdown",
    "onCommand:zetaNote.restartServer"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "zetaNote.restartServer",
        "title": "Zeta Note: Restart Server"
      },
      {
        "command": "zetaNote.showOutputChannel",
        "title": "Zeta Note: Show Output"
      }
    ],
    "configuration": {
      "title": "Zeta Note",
      "properties": {
        "zetaNote.customCommand": {
          "type": "string",
          "description": "When set use this command to run the language server.\nThe command is split on spaces: first part is the command name, the rest is the arguments.",
          "scope": "window"
        },
        "zetaNote.customCommandDir": {
          "type": "string",
          "markdownDescription": "When set run the `#zetaNote.customCommand#` from this dir rather than workspace root.",
          "scope": "window"
        },
        "zetaNote.trace.server": {
          "type": "string",
          "enum": [
            "off",
            "messages",
            "verbose"
          ],
          "default": "verbose",
          "description": "Level of verbosity in communicating with the server",
          "scope": "window"
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js",
    "vscode:prepublish": "webpack --mode production",
    "webpack": "webpack --mode development",
    "webpack-dev": "webpack --mode development --watch"
  },
  "dependencies": {
    "vscode-languageclient": "7.0.0",
    "which": "2.0.2"
  },
  "devDependencies": {
    "@types/glob": "^7.1.3",
    "@types/mocha": "^8.0.4",
    "@types/node": "^12.11.7",
    "@types/vscode": "^1.54.0",
    "@types/which": "1.3.1",
    "@typescript-eslint/eslint-plugin": "^4.14.1",
    "@typescript-eslint/parser": "^4.14.1",
    "eslint": "^7.19.0",
    "glob": "^7.1.6",
    "mocha": "^8.2.1",
    "ts-loader": "^8.0.18",
    "typescript": "^4.1.3",
    "vscode-test": "^1.5.0",
    "webpack": "^5.26.0",
    "webpack-cli": "^4.5.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/artempyanykh/zeta-note-vscode"
  }
}